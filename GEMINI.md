# Project Overview

This is a Rust project named "leetcode" dedicated to solving LeetCode problems. The project is organized into modules within the `src/lib/` directory, likely categorizing solutions by data structure, algorithm, or problem type. It uses the 2021 edition of Rust.

## Building and Running

To build, run, and test the project, use the standard Cargo commands:

*   **Build:** `cargo build`
*   **Run:** `cargo run`
*   **Test:** `cargo test`

## Development Conventions

The project structure suggests that solutions are organized into individual modules under `src/lib/` based on their category (e.g., `array_strings.rs`, `binary_search.rs`, `graphs.rs`). This modular approach helps in managing different types of LeetCode problems efficiently.